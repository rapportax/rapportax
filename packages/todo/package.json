{
  "name": "@rapportax/todo",
  "private": true,
  "version": "0.0.0",
  "description": "Todo intake and management surface",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "format": "biome format --write src",
    "test": "echo 'todo app: no tests yet'"
  }
}
