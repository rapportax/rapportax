{
  "name": "@rapportax/worker",
  "private": true,
  "version": "0.0.0",
  "description": "Worker that processes todo execution",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/run.ts",
    "run": "tsx src/run.ts",
    "monitor": "tsx src/monitor/server.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "format": "biome format --write src",
    "test": "echo 'worker: no tests yet'"
  },
  "dependencies": {
    "@openai/agents": "^0.4.1",
    "zod": "^4.0.0"
  }
}
